<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.adhi.amovia">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="false"
        android:icon="@drawable/launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AMOVIA">
        <activity
            android:name=".ui.favorite.FavoriteActivity"
            android:exported="true" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.AMOVIA.NoActionBar"
            android:windowSoftInputMode="adjustPan"/>
        <activity
            android:name=".ui.detail.DetailTvActivity"
            android:theme="@style/Theme.AMOVIA.AppBarOverlay" />
        <activity
            android:name=".ui.detail.DetailMovieActivity"
            android:theme="@style/Theme.AMOVIA.AppBarOverlay" />
        <activity
            android:name=".ui.splashscreen.SplashScreenActivity"
            android:exported="true"
            android:theme="@style/Theme.AMOVIA.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>